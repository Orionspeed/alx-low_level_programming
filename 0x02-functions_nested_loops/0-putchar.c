#include "main.h"

/**
 * main - program that prints _putchar, followed by a new line.
 *
 * Return: Always 0(Succes)
 */

int main(void)
{
	char x[] = "_putchar"
		int i;
	for (i = 0; x[i]; i++)
		_putchar(x[i]);
	
	_putchar('\n');
	return (0);
}
